import java.util.Scanner;

public class Animal {

    Scanner input = new Scanner(System.in);
}
/*
🔷 1. What is a Package in Java?

A package is a folder (directory) that groups related classes together.

👉 It helps organize your code like files in folders.

🔷 2. What is Classpath?

Classpath is a path (location) where Java looks for:
.class files
packages
libraries

👉 It tells Java:
👉 "Where should I find the compiled classes?
 */